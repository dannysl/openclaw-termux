<?xml version="1.0" encoding="utf-8"?>
<!-- dataExtractionRules for Android 12+ -->
<!--
  SharedPreferences hold the node Ed25519 private key, the paired device token
  and the gateway auth token. Those are device-bound credentials and must not
  leave the device, so the preferences file that stores them is excluded from
  both cloud backup and device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <include domain="sharedpref" path="." />
        <exclude domain="sharedpref" path="FlutterSharedPreferences.xml" />
    </cloud-backup>
    <device-transfer>
        <include domain="sharedpref" path="." />
        <exclude domain="sharedpref" path="FlutterSharedPreferences.xml" />
    </device-transfer>
</data-extraction-rules>
